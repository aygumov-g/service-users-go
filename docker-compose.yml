services:
  service-users-db:
    image: postgres:17-alpine
    container_name: users-db
    environment:
      - POSTGRES_DB=$POSTGRES_DB
      - POSTGRES_USER=$POSTGRES_USER
      - POSTGRES_PASSWORD=$POSTGRES_PASSWORD
    ports:
      - "$POSTGRES_PORT:5432"
    volumes:
      - pgdata-service-users:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
  migrate:
    image: migrate/migrate
    volumes:
      - ./migrations:/migrations
    command:
      [
        "-path", "/migrations",
        "-database",
        "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}/${POSTGRES_DB}?sslmode=disable",
        "up"
      ]
    depends_on:
      service-users-db:
        condition: service_healthy
    restart: "no"
  service-users-app-go:
    build:
      context: .
      dockerfile: Dockerfile
    image: aygumov-g/service-users-app-go:latest
    container_name: users-app-go
    environment:
      - APP_PORT=$APP_PORT
      - POSTGRES_HOST=$POSTGRES_HOST
      - POSTGRES_USER=$POSTGRES_USER
      - POSTGRES_PASSWORD=$POSTGRES_PASSWORD
      - POSTGRES_DB=$POSTGRES_DB
    ports:
      - "$APP_PORT:$APP_PORT"
    networks:
      - default
      - messenger-system
    depends_on:
      service-users-db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: always

volumes:
  pgdata-service-users:

networks:
  messenger-system:
    external: true
